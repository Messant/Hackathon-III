{% extends 'base.html.twig' %}
    {% block stylesheet %}
        <link rel="stylesheet" href="{{ asset('css/joueur_envoi_photo.css') }}">
    {% endblock %}

{% block content %}
    {#<img src="{{ asset('upload/' ~   photo )}}">#}
    <div class="container-fluid">
        <div class="row">
            <div class=" col-xs-12 well upload ">
                {{ form_start(form) }}
               <div class="hidden">{{ form_row(form.validated) }}</div>
                <input type="submit" value="Create"/>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}